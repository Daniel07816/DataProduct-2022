openapi: "3.0.3"
servers:
  description: Localhost
  url: http://127.0.0.1:80
info:
  description: API Description
  version: "1.0.0"
  title: Prediccion de clientes de credito
  contact:
    email: "cldelcid@ufm.edu"
    email: "danielbehar@ufm.edu"
paths:
  /connection-status:
    get:
      summary: 'Checks that our API service is running'
      responses:
        default:
          description: Default response.
  /predict:
    post:
      summary: 'Predice si varios clientes es bueno o malo para pagar basado en input JSON'
      responses:
        default:
          description: Default response.
      parameters: []
      requestBody:
        description: Stranded Patient Data
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                Age: 
                  type: number
                  title: edad de la persona 
                  example: 25
                Sex: 
                  type: factor
                  title: sexo
                  example: female
                Job: 
                  type: number
                  title: job
                  example: 2
                Housing: 
                  type: factor
                  title: housing
                  example: free
                Saving.accounts: 
                  type: factor
                  title: savingAccounts
                  example: little
                Checking.account: 
                  type: factor
                  title: cheking
                  example: little
                Credit.amount: 
                  type: number
                  title: Creditamount
                  example: 4308
                Duration: 
                  type: number
                  title: duration
                  example: 12
                Purpose: 
                  type: factor
                  title: purpose
                  example: business
  /batches:
    post:
      summary: 'Predice si varios clientes es bueno o malo para pagar basado en input JSON'
      responses:
        default:
          description: Default response.
      parameters: []
      requestBody:
        description: Stranded Patient Data
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                Age: 
                  type: number
                  title: edad de la persona 
                  example: 25
                Sex: 
                  type: factor
                  title: sexo
                  example: female
                Job: 
                  type: number
                  title: job
                  example: 2
                Housing: 
                  type: factor
                  title: housing
                  example: free
                Saving.accounts: 
                  type: factor
                  title: savingAccounts
                  example: little
                Checking.account: 
                  type: factor
                  title: cheking
                  example: little
                Credit.amount: 
                  type: number
                  title: Creditamount
                  example: 4308
                Duration: 
                  type: number
                  title: duration
                  example: 12
                Purpose: 
                  type: factor
                  title: purpose
                  example: business
  /metricas:
    get:
      summary: 'Retorna un png con las metricas del modelo'
      responses:
        default:
          description: Default response.
      parameters: []
                  
